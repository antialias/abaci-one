/**
 * Toys layout - minimal wrapper with no GameModeProvider.
 * Toys are standalone interactive explorations, not games.
 */
export default function ToysLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
