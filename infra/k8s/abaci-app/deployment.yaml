# Application Deployment
#
# Managed by ArgoCD (replacing Terraform management)
# ConfigMap and Secret are still managed by Terraform

apiVersion: apps/v1
kind: Deployment
metadata:
  name: abaci-app
  namespace: abaci
  labels:
    app: abaci-app
spec:
  # 2 replicas: on a single-node cluster this buys zero-downtime rolling deploys
  # (maxSurge:1), not node-failure HA (which one node can't provide anyway).
  replicas: 2
  selector:
    matchLabels:
      app: abaci-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: abaci-app
    spec:
      securityContext:
        fsGroup: 1001
      # Note: No init container - migrations run via ArgoCD PreSync hook
      containers:
        - name: app
          image: ghcr.io/antialias/abaci-one:latest
          command: ["node", "--require", "./instrumentation.js", "server.js"]
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-env
          resources:
            # cpu request sized to observed 10-day peak (~60m/replica) + headroom.
            # limit left generous so SSR/startup bursts aren't throttled.
            requests:
              memory: "512Mi"
              cpu: "75m"
            limits:
              memory: "1Gi"
              cpu: "2000m"
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 5
          volumeMounts:
            - name: vision-training-data
              mountPath: /app/apps/web/data/vision-training
            - name: uploads-data
              mountPath: /app/apps/web/data/uploads
            - name: audio-data
              mountPath: /app/apps/web/data/audio
      volumes:
        - name: vision-training-data
          persistentVolumeClaim:
            claimName: vision-training-data
        - name: uploads-data
          persistentVolumeClaim:
            claimName: uploads-data
        - name: audio-data
          persistentVolumeClaim:
            claimName: audio-data
